<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="exam">
  <select id="getRecruitTitle" parameterType="String" resultType="RecruitInfoDTO" useCache="true">
    select r.title as title, r.start_date as startDate, r.end_date as endDate
    from applicant as ap
    join recruitment as r
    on ap.recruitment_id = r.id
    where ap.apt_id = #{aptId}
  </select>
  <select id="searchRecruitId" parameterType="Integer" resultType="Integer" useCache="true">
    select recruitment_id from exam where id = #{examId}
  </select>
  <select id="getExamination" parameterType="Integer" resultType="ExaminationDTO" useCache="true">
    select question, answer from exam where recruitment_id = #{recruitId}
  </select>
</mapper>