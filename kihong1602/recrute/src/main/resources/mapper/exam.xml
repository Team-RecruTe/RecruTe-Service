<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="exam">
  <select id="getRecruitTitle" parameterType="Integer" resultType="ExamDTO">
    select r.title as title, r.start_date as startDate, r.end_date as endDate
    from exam e join recruitment r on e.recruitment_id = r.id
    where e.id = #{examId}
  </select>
</mapper>