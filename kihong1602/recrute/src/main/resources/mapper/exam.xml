<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="exam">
  <select id="getRecruitTitle" parameterType="Integer" resultType="ExamInfoDTO">
    select title, start_date as startDate, end_date as endDate from recruitment
    where id = #{recruitId}
  </select>
  <select id="searchRecruitId" parameterType="Integer" resultType="Integer">
    select recruitment_id from exam where id = #{examId}
  </select>
  <select id="getExamination" parameterType="Integer" resultType="ExaminationDTO">
    select question, answer from exam where recruitment_id = #{recruitId}
  </select>
</mapper>