<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.jisun.mybatis"> 
	<insert id="insertMember"
			parameterType="MemberDto">
	insert into member (member_id,password,email,name,birth,gender,phone_number)
        		values (#{memberID},
		        		#{password},
		        		#{email},
		        		#{name},
		        		#{birth},
		        		#{gender},
		        		#{phoneNum})
	</insert>
			
			
	<select id="loginMember"
			resultType="MemberDto" 
			parameterType="MemberDto"> 
	select member_id ,password,name,birth,gender,phone_number,email 
	from member 
	where member_id = #{memberID} and password = #{password}
	</select>
	
</mapper>